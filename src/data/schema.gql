
enum State {
  active
  deleted
}

enum AccountType {
  user
}

type Installation {
  installationId: Int!
  targetId: Int!
  targetType: AccountType!
  repositoryId: Int!
  state: State!
}

type Query {
  installationByRepositoryId(repositoryId: Int!): Installation
}
